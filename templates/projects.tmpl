<div class="projectname">
{{if .Url}}<a target="_blank" href="{{.Url}}">{{end}}
	{{.Name}}
	{{if .Url}}</a>{{end}}
</div>
{{if .Skills}}
	<small>Skills:</small>
		<ul>
			{{range .Skills}}
				<li><a href="/searchSkill?skills={{.}}">{{.}}</a></li>
			{{end}}
		</ul>
{{end}}
